{
  "name": "sns_pt_dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "webpack serve --mode development",
    "build": "webpack --mode production",
    "update-csv": "cd scripts_history && python atualizar_dados_sns.py && python atualizar_tabelas_fact.py && cd ..",
    "test": "echo ok",
    "obfuscate": "npm run build && node -e \"const fs = require('fs'); const path = require('path'); const jsFiles = fs.readdirSync('dist').filter(f => f.startsWith('bundle.') && f.endsWith('.js') && !f.includes('.map') && !f.includes('.LICENSE')); if (jsFiles.length > 0) { const file = jsFiles[0]; const { execSync } = require('child_process'); console.log('Obfuscating:', file); execSync(`npx javascript-obfuscator dist/${file} --output dist/${file} --compact true --debug-protection true --options-preset high-obfuscation`, { stdio: 'inherit' }); console.log('Obfuscation completed!'); } else { console.error('No main JS file found'); process.exit(1); }\"",
    "obfuscate:dev": "node -e \"const fs = require('fs'); const path = require('path'); try { const jsFiles = fs.readdirSync('dist').filter(f => f.startsWith('bundle.') && f.endsWith('.js') && !f.includes('.map') && !f.includes('.LICENSE')); if (jsFiles.length > 0) { const file = jsFiles[0]; const { execSync } = require('child_process'); console.log('Obfuscating:', file); execSync(`npx javascript-obfuscator dist/${file} --output dist/${file} --compact true --debug-protection true --options-preset high-obfuscation`, { stdio: 'inherit' }); console.log('Obfuscation completed!'); } else { console.error('Build directory not found. Run npm run build first.'); process.exit(1); } } catch (error) { if (error.code === 'ENOENT') { console.error('Build directory not found. Run npm run build first.'); } else { console.error('Error:', error.message); } process.exit(1); }\""
  },
  "keywords": [
    "sns",
    "sns_pt",
    "sns_data"
  ],
  "author": "--=FurtiF™=--",
  "license": "ECL-2.0",
  "bugs": {
    "url": "https://github.com/Furtif/sns_dashboard_web/issues"
  },
  "homepage": "https://github.com/Furtif/sns_dashboard_web#readme",
  "funding": [
    {
      "type": "GitHub Sponsors",
      "url": "https://github.com/sponsors/Furtif"
    },
    {
      "type": "Paypal",
      "url": "https://www.paypal.com/donate/?hosted_button_id=WXA874KBC7RNN"
    }
  ],
  "type": "commonjs",
  "dependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.2",
    "@babel/preset-react": "^7.28.5",
    "babel-loader": "^10.1.1",
    "css-loader": "^7.1.4",
    "html-webpack-plugin": "^5.6.7",
    "papaparse": "^5.5.3",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "recharts": "^3.8.1",
    "style-loader": "^4.0.0",
    "webpack": "^5.106.2",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.3"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^14.0.0",
    "javascript-obfuscator": "^5.4.1"
  }
}
